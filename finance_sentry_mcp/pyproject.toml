[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "finance-sentry-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
]

[project.scripts]
finance-sentry-mcp = "finance_sentry_mcp.__main__:main"
