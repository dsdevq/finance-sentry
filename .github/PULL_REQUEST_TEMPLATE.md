<!-- Title: conventional commit — release-please parses it into the CHANGELOG.
     Scope = module/package/spec number, e.g. feat(subscriptions): …, fix(040): … -->

Refs #<!-- issue number -->

## What

<!-- What this PR changes. -->

## Why

<!-- Why the change exists — the problem or the destination it traces to. -->

## Validation

<!-- Exactly what you ran and its result. e.g.:
- `cd frontend && npm run lint && npm run format:check` — clean
- `cd frontend && npm run test:ci` — N specs green, coverage floors met
- `dotnet test FinanceSentry.sln --filter "Category!=Integration"` — N tests green
-->
